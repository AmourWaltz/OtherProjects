#ifndef _NVIC_H
#define _NVIC_H

#include "common.h"
#include "include.h"

/*===============================================================
        @note      人生若只如初见，何事秋风悲画扇；
                   等闲变却故人心，却道故人心易变；
                   骊山语罢清宵半，泪雨霖铃终不怨；
                   何如薄幸锦衣郎，比翼连枝当日愿。
==================================================================*/

//如果测试抢占式中断，则定义为1，否则为0
#define PREEMPTION      1

void NVIC_Init (void);//初始化中断控制

#endif